name: Validate outlet switch

on:
  pull_request:
    branches: [main]
    paths:
      - 'src/outletSwitchLab.ts'
      - 'e2e/outlet-switch.spec.ts'
      - 'src/main.ts'
      - '.github/workflows/validate-outlet-switch.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  outlet-switch:
    runs-on: ubuntu-24.04
    timeout-minutes: 8
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: package.json

      - name: Install dependencies
        run: npm install --no-audit --no-fund

      - name: Install Chromium
        run: npx playwright install --with-deps chromium

      - name: Build production bundle
        run: npm run build

      - name: Verify physical outlet switch only
        run: npx playwright test e2e/outlet-switch.spec.ts --workers=1 --timeout=20000
