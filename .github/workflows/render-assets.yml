name: Render Blender assets

on:
  workflow_dispatch:
  push:
    paths:
      - 'tools/blender/**'
      - '.github/workflows/render-assets.yml'

permissions:
  contents: read

jobs:
  render:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    env:
      FORGEKIT_LIMIT: '1'

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Blender and virtual display
        run: |
          sudo apt-get update
          sudo apt-get install -y blender xvfb
          blender --version

      - name: Render one ForgeKit smoke-test part
        id: render_forgekit
        continue-on-error: true
        shell: bash
        run: |
          set -o pipefail
          xvfb-run -a -s '-screen 0 1280x1024x24' \
            blender --background --factory-startup --python tools/blender/run_forgekit_compat.py \
            2>&1 | tee forgekit-render.log

      - name: Upload render diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: forgekit-render-log
          path: forgekit-render.log
          if-no-files-found: warn
          retention-days: 14

      - name: Stop after renderer failure
        if: steps.render_forgekit.outcome != 'success'
        run: |
          echo 'ForgeKit renderer failed. Last 160 log lines:'
          tail -n 160 forgekit-render.log || true
          exit 1

      - name: Verify smoke-test output
        run: |
          test -d generated-assets/forgekit
          test -d generated-assets/forgekit/icons
          test -f generated-assets/forgekit/manifest.json
          find generated-assets/forgekit -maxdepth 1 -name '*.png' -print
          find generated-assets/forgekit/icons -maxdepth 1 -name '*.png' -print
          test "$(find generated-assets/forgekit -maxdepth 1 -name '*.png' | wc -l)" -eq 1
          test "$(find generated-assets/forgekit/icons -maxdepth 1 -name '*.png' | wc -l)" -eq 1

      - name: Upload ForgeKit smoke-test asset
        uses: actions/upload-artifact@v4
        with:
          name: forgekit-smoke-test-beam-3
          path: generated-assets/forgekit
          if-no-files-found: error
          retention-days: 14
